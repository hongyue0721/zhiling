# 知径（Zhijing）项目目标

## 1. 项目定位

知径是一款基于知乎真实内容构建学习路径的 Web 教育产品。

用户输入一个想学习的主题后，系统通过知乎开放 API 获取相关问题、回答、文章与观点信号，将散落的内容组织成一张有先后关系的学习地图。用户可以查看原始来源、理解同一问题下的共识与分歧，并通过有来源依据的题目验证学习效果。

产品不是“知乎内容总结器”，也不替代原作者表达。AI 的职责是组织、导航、比较和验证；所有知识结论都必须能够追溯到真实来源。

核心表达：

> 把知乎上的真实讨论，走成一条学会的路。

## 2. 目标用户与使用场景

目标用户是希望系统学习某个主题，但面对大量零散内容时不知道从哪里开始的普通学习者。

核心场景：

1. 用户注册或登录后，输入学习目标，或选择经过检查的精选主题。
2. 系统生成包含 5–7 个节点的学习地图。
3. 用户按照先修关系依次学习节点。
4. 每个节点展示学习目标、知乎来源、共识、分歧和实践经验。
5. 用户完成基于来源内容生成的客观题或观点辨析题。
6. 系统保存学习进度，并在完成后生成仅本人可读的认知报告。

## 3. 产品原则

### 3.1 来源优先

- 每条知识结论必须关联一个或多个真实来源。
- 不接受由模型生成的 URL。
- 来源不足时明确显示“材料不足”，不使用模型常识补齐并伪装成知乎内容。
- 页面保留原文入口，鼓励用户回到知乎阅读完整表达。

### 3.2 多视角学习

- 不把复杂讨论粗暴处理成单一标准答案。
- 内容组织为共识、分歧、实践经验和条件差异。
- 分歧必须展示观点依据、适用条件及对应来源。

### 3.3 学完必须验证

- “看过”不等于“学会”。
- 节点完成由答题结果驱动，不能仅由前端手动标记。
- 第一阶段采用单选、多选、匹配和观点辨析，不使用不稳定的开放式主观评分；题目集必须完整覆盖地图全部节点，每节点 2–3 题，匹配题选项由服务端保存互斥的 `left`/`right` 两侧。

### 3.4 必须登录

- 所有产品能力都要求用户先注册或登录，不提供匿名学习模式。
- 未认证访问者只能进入注册和登录流程，不能读取精选地图、生成地图、答题或访问个人进度。
- 学习关系、答题记录和进度在创建时即归属于唯一正式账户，不进行匿名数据迁移或账户合并。
- 登录成功后，用户可以跨设备恢复个人地图、学习进度和答题记录。

### 3.5 演示稳定性也是产品能力

- 精选主题必须提前生成、人工检查并持久化。
- 自定义主题使用真实知乎 API 和模型生成。
- 外部接口不可用时，精选主题仍须完整可体验。
- 不用伪造的实时结果掩盖真实接口失败。

## 4. 第一阶段功能范围

### 4.1 首页

- 登录成功后进入产品首页。
- 产品定位与简短使用说明。
- 3–5 个经过检查的精选学习主题。
- 自定义学习主题输入。
- 清晰区分“精选地图”和“现场生成”。

### 4.2 学习地图

- 展示里程碑、知识点和观点分支。
- 支持节点选择、缩放、平移和稳定布局。
- 节点展示标题、目标、完成状态、来源数量和分歧提示。
- 详细内容在节点面板中展示，不将大段文字堆在画布上。

### 4.3 来源与观点

- 展示知乎内容标题、摘要、链接、作者及公开可信度信号。
- 支持共识、分歧、实践经验和补充材料分类。
- 每条结构化观点保留来源关联。

### 4.4 学习验证

- 每个节点提供 2–3 道有来源依据的题目。
- 服务端判题，不向浏览器提前下发正确答案。保存不可变尝试记录和最佳成绩；每题等权，节点最佳成绩达到 80% 时完成，并允许不限次数重试。同一关系、题集、节点及幂等键重复提交返回同一结果；相同幂等键跨节点不串用，最佳成绩只取最大值且完成状态不回退。

### 4.5 账户与进度

- 邮箱密码注册与登录。
- 所有产品页面和业务接口的登录门禁。
- 学习地图加入记录。
- 节点进度和答题历史。
- 跨会话、跨设备的学习数据恢复。

### 4.6 结课报告

- 展示完成度、薄弱节点、接触过的不同观点及下一步建议。
- 报告只允许所属学习关系的账户读取。

## 5. 明确不在第一阶段实现

- 付费、会员、套餐和计费系统。
- 面向用户的 API 额度管理界面。
- 知乎收藏夹、关注和创作数据 OAuth。
- 学习社区、排行榜与多人协作。
- 自动复习通知和复杂遗忘曲线系统。
- 移动 App。
- 任意深度递归支线。
- 全量抓取知乎内容。
- 自建向量数据库或本地模型训练。
- 模型对开放式长答案进行主观打分。
- 报告分享、分享快照、分享链接和指定接收者授权。

## 6. 技术方向

### 6.1 应用技术栈

- Next.js 15
- React
- TypeScript
- Tailwind CSS
- `@xyflow/react`
- Zustand
- Zod
- Server-Sent Events

- Next.js 15 模块化单体承载 Web/API/SSE；同仓库同镜像的 generation Worker 作为独立进程，通过 PostgreSQL 租约执行长时任务。Web 进程只处理请求、SSE 与持久化状态，不读取知乎 Access Secret；Worker 是唯一调用知乎搜索和知乎直答的进程，Web 与 Worker 共享同一模块和公开契约。
### 6.2 认证

- Better Auth
- Email & Password

知径用户认证与知乎开放平台鉴权严格分离。知乎 Access Secret 只保存在服务端环境变量中，不进入浏览器、数据库、公开日志或用户 Session。

### 6.3 数据库

- PostgreSQL
- Drizzle ORM
- Drizzle migrations

主要业务实体：

- 用户、账户与会话。
- 学习地图与地图节点。
- 知识来源及节点来源关系。
- 有依据的结构化观点及观点来源关系。
- 验证题、选项和答题记录。
- 用户选课记录和节点进度。
- 地图生成任务。

一张学习地图可以被多个用户学习。用户学习关系和进度独立保存，不为每个用户复制整张地图。

### 6.4 外部适配层

知乎 API 与模型 API 均通过独立适配器接入，业务层不直接依赖第三方响应结构。

知乎适配器负责：

- Bearer 鉴权和请求时间戳。
- 按现行官方统一 HTTP 契约完成字段转换和请求/错误映射；当前没有独立“正式接口/比赛接口”端点或 DTO，不伪造第二套适配器。若未来出现独立赛事契约，新增兼容登记后再实现。
- 错误码映射。
- 结果标准化。
- 删除不可信或缺少必要字段的内容。

模型适配器负责：

- 学习方向规划。
- 基于真实搜索结果构建节点。
- 共识、分歧与实践经验提取。
- 验证题生成。
- 结构化输出解析。

## 7. 地图生成流程

采用“两阶段混合”生成方法：

1. 规范化用户输入的学习目标。
2. 查询是否存在同版本缓存地图。
3. 模型生成 3–4 个粗粒度学习方向。
4. 每个方向分别调用知乎搜索。
5. 对结果进行去重、排序和可信度整理。
6. 根据真实内容重建最终 5–7 个学习节点。
7. 仅对材料不足的节点补充搜索一次。
8. 提取有来源关联的共识、分歧和实践经验。
9. 校验每条结论的来源 ID、节点归属和原始链接。
10. 生成基于来源的客观题。
11. 在完整校验通过后持久化地图。

自定义地图生成过程通过 SSE 报告阶段进度，但未经校验的节点内容不得提前作为正式结果展示。

## 8. 缓存与外部调用保护

项目不实现付费或用户额度产品，但保留必要的工程保护：

- 相同主题、相同生成版本优先复用缓存。
- 同一主题的并发生成请求合并为一个任务。
- 对正式账户设置由 PostgreSQL 持久化支持的生成频率限制；加入活动任务或命中缓存的幂等重试不重复计数。
- 精选地图完全依赖持久化数据，不依赖现场外部调用。
- API 失败时返回明确状态，不生成虚假地图。

缓存键至少包含：

```text
normalizedTopic + pipelineVersion + sourceAdapterVersion + modelAdapterVersion
```

## 9. 数据与安全边界

- 前端请求不接受或传递用户 ID，服务端从 Session 获取身份。
- 所有写操作校验资源所有权。
- 正确答案不随题目正文发送给浏览器。
- 密码交由认证框架安全散列，不自行实现密码算法。
- 不长期保存无必要的第三方完整原文。
- 结课报告只从服务端学习事实生成，并且只允许所属学习关系的账户读取。
- 所有产品页面和业务接口默认拒绝未认证访问；注册和登录端点除外。

## 10. 成功标准

产品完成时必须满足：

1. 新用户能够在三分钟内完成注册或登录，并开始体验精选地图的核心学习路径。
2. 精选主题可以稳定完成“地图 → 来源 → 多观点 → 答题 → 报告”闭环。
3. 自定义主题使用真实知乎 API，生成过程具有明确进度和失败状态。
4. 所有知识结论和题目解释均能追溯到系统取得的真实来源。
5. 未登录访问无法读取或操作精选地图、生成任务、题目、进度和报告等产品能力。
6. 用户重新登录后能恢复学习地图、进度和答题记录。
7. 结课报告只基于服务端保存的学习事实生成，并且错误账户无法读取。
8. 知乎 Access Secret 不出现在客户端构建产物、响应、日志或数据库中。
9. 外部 API 或模型暂时不可用时，精选演示路径仍然可用。
10. 核心业务逻辑具有可读、可维护的自动化测试。

## 11. 后续工作边界

本文件只定义项目目标、范围和技术方向，不定义工程实现细节。已落地实现须遵循对应工作包和已接受契约；新增业务实现、数据表、迁移或公开接口仍须先完成相应工作包与决策评审。
