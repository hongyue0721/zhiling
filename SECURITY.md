# 安全策略

## 当前支持范围

知径尚未发布正式版本。安全问题仍需按照本策略处理，不能因为项目处于开发阶段而提交真实密钥、生产数据或可利用细节。

## 报告方式

不要通过普通 Issue、PR 评论或提交信息披露未修复漏洞、令牌、密钥和个人数据。

优先使用 GitHub Security Advisory 的私有报告渠道；若仓库当前未启用该能力，请通过团队已经建立的私密沟通渠道联系仓库所有者。未建立私密渠道前，不要把敏感复现材料上传到仓库。

报告至少包含：

- 受影响的边界、版本或提交；
- 可重复的最小复现步骤；
- 已观察到的影响和必要前置条件；
- 不含真实密钥、密码和用户数据的证明材料；
- 报告者可接受的后续联系方法。

## 处理规则

- 先确认影响和遏制方式，再在私有分支修复和验证；
- 泄露凭据必须先撤销或轮换，删除 Git 历史中的文本不能恢复凭据安全性；
- 修复必须覆盖根因、回归测试、受影响版本和必要迁移；
- 在修复可用前，不在公开渠道披露可利用细节；
- 完成后同步更新安全边界、运行说明和 CHANGELOG。

## 项目安全边界

认证、资源授权、第三方密钥、来源内容、模型输出、题目答案和分享快照的强制规则见 [安全边界](docs/architecture/security-boundaries.md) 与 [访问控制矩阵](docs/architecture/access-control.md)。
