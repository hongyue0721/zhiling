# AGENTS.md

## 项目通用规范

### 核心理念

- 严禁以补丁最小、改动行数最少或暂时通过测试为目标；修复必须从根因闭合。
- 代码追求易维护、易读、可扩展，而非仅完成最小可行产品。
- 可读性优先，扩展点应建立在已确认需求之上，不得臆造抽象。
- 禁止用“看似健壮”的技术兜底掩盖业务事实缺失。

### 代码与架构

- 命名清晰自文档化，函数职责单一，注释说明“为什么”。
- 分层清晰、关注点分离，面向接口与抽象，避免硬编码依赖。
- 合理使用设计模式，不做脱离实际需求的过度设计。
- 核心逻辑必须有可读、可维护的测试覆盖。

### 开发流程

1. 先确认需求与业务事实，非 trivial 任务先规划再编码。
2. 实现代码、测试、相关文档与 API 契约必须同步提交。
3. 完成后执行构建和测试；发现问题后修复并重新验证。
4. 只实施任务范围内的改动，不附带无关重构。

### 修改前自检

- 这个值是事实，还是可以配置出来的策略？
- 缺失时应该失败，还是明确允许默认？
- 兜底是否只覆盖一个明确场景？
- 是否把某个特例伪装成了通用能力？
- 是否可能创建出“看起来成功但关系不一致”的数据？

### 执行准则

- 以瞎猜接口为耻，以认真查询为荣。
- 以模糊执行为耻，以寻求确认为荣。
- 以臆想业务为耻，以复用现有为荣。
- 以创造接口为耻，以主动测试为荣。
- 以跳过验证为耻，以人类确认为荣。
- 以破坏架构为耻，以遵循规范为荣。
- 以假装理解为耻，以诚实无知为荣。
- 以盲目修改为耻，以谨慎重构为荣。
