# Shittim Bookish V4 · 真实执行验证结果

本文记录本次接入实际仓库后的真实执行结果。验收不以离线预览截图代替整站
测试：所有命令都在仓库内实际运行，浏览器检查用真实 headless Chromium 对
本地 dev 服务器 + 本地 Postgres（含种子精选地图）执行。

## 1. 静态与自动化检查（最终提交树，全部真实执行）

| 检查 | 结果 |
| --- | --- |
| `pnpm format:check` | 通过（Prettier） |
| `pnpm lint` | 通过（eslint，--max-warnings 0） |
| `pnpm typecheck` | 通过（tsc --noEmit） |
| `pnpm architecture` | 通过（边界校验） |
| `pnpm test:unit` | 80/80 通过 |
| `pnpm test:architecture` | 10/10 通过 |
| `pnpm test:contracts` | 84/84 通过（含 runtime.contract 26 项、generation progress 3 项） |
| `pnpm test:integration` | 44/47 通过；3 项失败经在纯净基线 8c9d704 复测为完全一致的环境性失败，非本次引入 |
| `pnpm build` | 通过（Next.js 15.5.25 production build，全部路由） |

## 2. 真实浏览器验收（headless Chromium + dev DB，非截图）

登录：通过真实 `/api/auth/sign-up/email` + 标记 `emailVerified` + 真实
sign-in 建立会话；学习关系通过真实
`/api/featured-learning-maps/demo-featured-map/learning-relationship` 创建。

| 视口 | 检查项 | 结果 |
| --- | --- | --- |
| 1440×900 | 首页封面渲染 | 通过 |
| 1440×900 | 封面无横向溢出 | 通过 |
| 1440×900 | 昼夜主题切换并持久 | 通过 |
| 1440×900 | 藏书抽屉展示精选卷册 | 通过 |
| 1440×900 | 抽屉 Esc 关闭 | 通过 |
| 1440×900 | 图谱加载（题签标题） | 通过 |
| 1440×900 | 顶栏/题签/图谱物理分区无重叠（topbar 0–96，题签 117–218，图谱 231–900） | 通过 |
| 1440×900 | 图谱含 5 核心章节 + 10 处观点/引文旁注 | 通过 |
| 1440×900 | 点核心章节 → 打开读本（学习目标、第 1 章共识观点） | 通过 |
| 1440×900 | 点旁注 → 定位读本 | 通过 |
| 1440×900 | 点“练习”后题目惰性加载（1/3 单选等） | 通过 |
| 1440×900 | 提交作答、推进 | 通过 |
| 1440×900 | 浮层关闭后焦点回到触发按钮 | 通过 |
| 1440×900 | 无页面运行时错误 | 通过 |
| 390×844 | 封面无横向溢出 | 通过 |
| 390×844 | 图谱无横向溢出 | 通过 |
| 320×568 | 封面无横向溢出 | 通过 |
| 844×390（横屏） | 图谱无横向溢出 | 通过 |

### Three.js 真实路径与降级

- 正常环境：页面挂载 `data-renderer="three-webgl"`，`webgl-supported=true`
  （真实 GPU/WebGL 路径）。
- 以 `--disable-webgl --disable-gpu` 启动后：`data-renderer="canvas2d"`，
  Canvas2D 降级路径接管（与 WebGL 共享同一纸页形态函数）。

### 生成与 SSE

- 提交真实主题后，前端对 `/api/map-generations` 发起真实 POST，进入阶段
  状态（示例截图文字：`In the making | … | 准备出发`），SSE 阶段展示真实。
- 无 worker 消费时任务停留 queued（真实语义），前端保持连接并展示
  准备/重连/失联状态——这是对 SSE 中断-恢复逻辑的真实冒烟。
- 生成成功→读取快照→进入地图的真实闭环依赖知乎 provider 与 worker，
  本机未配置密钥故未在本次执行；该项与“超时后恢复/缓存命中”一并列为
  仍未在本机完成、需在带 provider 的环境复验。

## 3. 版本核对结论

- 本次审阅返回的 main：`f808785…`；后端 runtime.ts blob：`b362b84…`。
- 实际仓库 `dev` HEAD（`8c9d704…`）中该文件 blob 恰为 `b362b84…`，与 V4
  `runtime.json` 的 `base_blob_sha` 一致。
- 因此采用“按职责语义迁移”而非整文件覆盖；已修复问题跳过并记录依据
  （structure 阶段不恢复 excerpt，依据 8d17bfa 的线上拒答修复及其回归测试）。

## 4. 仍未在本机验证的部分（如实汇报）

1. 成功生成后读取快照进入地图、缓存命中、SSE 断线重连后的完整成功链
   —— 需要真实知乎 provider 密钥与运行中的 generation worker。
2. 5 节点与 7 节点的“较多节点”图谱与长标题/空观点/单来源/多来源的视觉
   压力组合：本机种子图为固定 5 节点；代码为纯布局投影（无新请求），
   建议在带 provider 环境用真实生成任务复验长标题与 7 节点。
3. E2E（tests/e2e）整套：同样依赖 provider/worker 与隔离测试库。
4. 集成测试 3 项环境性失败（与纯净基线一致），未在本机修复。

## 5. 未把离线预览当整站通过

- `Shittim_Bookish_Preview_V4.html` 与 `preview/` 仅用于视觉参考，未作为
  生产路由，未复制其 `preview/app.js`。
- 全部页面验收在真实 Next dev server + 真实 DB + 真实浏览器完成。
