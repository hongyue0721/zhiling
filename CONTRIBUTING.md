# 贡献指南

## 开始工作

1. 从最新的 `dev` 创建短生命周期分支。
2. 在编码前确认任务边界、验收条件和受影响契约。
3. 非简单改动先在 Issue 或设计文档中记录方案。

领取任务前先检查 [协作任务队列](docs/development/task-board.md) 的前置依赖和状态。状态为“阻塞”或“待决策”的工作不得提前实现。

建议分支名：`feature/<scope>`、`fix/<scope>`、`docs/<scope>`、`refactor/<scope>`。

所有提交必须使用中文标题，并通过正文解释功能、原因和验证结果。类型标识可保留社区通用缩写，但功能描述必须使用中文。

```text
<类型>(<范围>): <中文功能标题>

功能：说明本次实现或调整了什么。
原因：说明业务背景、问题根因或设计依据。
验证：列出实际执行的检查及其结果。
```

`类型` 可取 `feat`、`fix`、`docs`、`test`、`refactor`、`build`、`ci`、`chore`。禁止使用“update files”“修改一下”等无法说明成果的标题，也不能省略正文中的功能解说。

提交 PR 前执行：

```bash
scripts/verify-collaboration.sh --base origin/dev --head HEAD
```

普通 PR 必须以 `dev` 为目标分支；`main` 只接受 `dev-to-main` Bot 的提升 PR。

## 合入要求

每个普通 PR 必须：

- 目标为 `dev`，范围单一，并说明原因、方案和验证结果；
- 关联已完成前置依赖的 Issue，必要时关联已接受 ADR；
- 为核心行为提供测试；
- 同步更新受影响的产品、架构、开发或 API 文档；
- 明确兼容性影响、迁移方式和回滚方式；
- 不得提交密钥、令牌、生产数据或本地环境文件；
- 在 `协作规范检查` 与 `工程质量检查` 成功、所有者评审和独立评审完成后，才能由维护者 rebase 合入 `dev`。

API 发生变化时，必须先更新 `api/openapi.yaml` 及相关 schema、示例和错误定义。破坏性变更必须有版本策略和迁移窗口，不能静默合入。

禁止普通 PR 直接指向 `main`，也禁止人工直接推送 `dev` 或 `main`。`dev` 合入后由 `dev-to-main` Bot 等待对应工作流完成，再创建并自动 rebase 合入 `main`；Bot 不得绕过失败、运行中或超时的检查。

## 评审重点

- 业务事实是否真实且关系一致；
- 模块职责和依赖方向是否清晰；
- 失败是否显式，是否存在掩盖数据缺失的默认值；
- 测试是否覆盖正常路径、边界和明确的失败路径；
- 文档是否足以让其他成员独立理解和验证变更。
