# 测试策略

技术栈确认后，应将测试命令固化为本地与 CI 共用入口。

最低测试层次：

- **单元测试**：领域规则、值转换及明确失败路径；
- **契约测试**：API、模块接口和第三方适配器；
- **集成测试**：真实数据库或关键基础设施边界；
- **端到端测试**：少量关键业务流程，不替代底层测试。

缺陷修复必须包含能够在修复前复现问题的回归测试。兼容层必须分别验证支持版本、边界值、信息损失和拒绝路径。
